{
  "name": "falconfind",
  "version": "1.0.0",
  "private": true,
  "description": "FalconFind - Fanshawe College Lost & Found System (Angular + Node + Firebase)",
  "scripts": {
    "dev": "concurrently \"npm run dev:front\" \"npm run dev:back\"",
    "dev:front": "node scripts/generate-frontend-env.mjs && cd frontend && ng serve",
    "dev:back": "cd backend && NODE_OPTIONS=--disable-warning=DEP0040 tsx index.ts",
    "build": "npm run build:front && npm run build:back",
    "build:front": "node scripts/generate-frontend-env.mjs && npm run build --prefix frontend",
    "build:back": "cd backend && npm run build",
    "lint": "npm run lint:front && npm run lint:back",
    "lint:front": "cd frontend && npm run lint",
    "lint:back": "cd backend && npm run lint",
    "format": "prettier --write .",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "@types/multer": "^2.0.0",
    "concurrently": "^8.2.2",
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=24.11.1"
  },
  "dependencies": {
    "multer": "^2.0.2",
    "tsx": "^4.21.0"
  }
}
